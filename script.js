// Select the scroll-top button
const scrollTopButton = document.getElementById("scroll-top");

// Add an event listener to the button
scrollTopButton.addEventListener("click", () => {
    // Scroll to the top of the page
    window.scrollTo(0, 0);
});
